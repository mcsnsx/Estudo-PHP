<?php

//NÃO SE USA <BR>, ESTOU USANDO AQUI PARA FINS EDUCACIONAIS

echo "CONCEITO DE PROGRAMAÇÃO ORIENTADA A OBJETOS - POO";
//é um estilo de programação 

echo "<br><br>";

echo "<b>O que é uma classe?</b>
      <p>É um conjunto de atributos e métodos. Funciona como uma caixa e o que 
      importa é o que está dentro dela, se refere a diferentes assuntos, cada 
      classe agrupa um 'assunto' dentro dela onde só será 'discutido aquele assunto'.
      Dentro das classes, as variáveis e funções gnham alguns recursos. Quando for 
      preciso armazenar valores dentro de uma classe, pode-se criar um atributo."
;

echo "<br><br>";

echo "<b>O que é um atributo?</b>
      <p>Funciona como uma variável mas com recursos a mais. Quando uma variável está
      dentro de ua classe, ela passa a ser um atributo, isso é chamado de 'Encapsulamento'.
      É onde eu deixo as informações quando eu preciso armazena-las."
;

echo "<br><br>";

echo "<b>O que é um encapsulamento?</b>
      <p>É uma forma de controle de acesso sobre as funções e variaveis/atributos dentro da 
      classe."
;

echo "<br><br>";

echo "<b>O que é um método?</b>
      <p>É como uma função. É onde eu deixo as informações quando eu preciso executa-las. "
;

echo "<br><br>";

echo "<b>O que é um objeto?</b>
      <p>É uma variável que representa ou copia a classe, é representada a partir da palavra
      'new' que é o que cria/inicia a classe de fato."
;

echo "<br><br>";

echo "<b>O que é um instanciamento?</b>
      <p>Quando vamos usar uma classe, precisamos fazer o instanciamento que é a forma de
      representar/chamar a classe, funciona como uma referencia/especificação, faz a classe
      funcionar "
;

echo "<br><br>";

echo "<b>O que é herança?</b>
      <p>É quando uma classe herda informações de outra classe"
;

echo "<br><br>";

echo "<b>O que são métodos mágicos?</b>
      <p>São coisas que acontecem fora da nossa vista, quando os
      metodos foram chamados automáticamente, depois que já foram 
      criados, todos os métodos mágicos começam com o uso de dois
      '_' (underline) no inicio, ou '__construction'."  
;

echo "<br><br>";

echo "<b>O que são métodos construtores?</b>
      <p>São métodos que são executados automaticamente quando a 
      classe é instanciada, o método deve ter exatamente o mesmo
      nome da classe."
;

echo "<br><br>";

echo "<b>Quem pode acessar atributos e metodos?</b>
      <p>A própria classe, classes extendidas [herdeiras] e objetos. 
      Quando é público, todo mundo vê. Quando é protegido, só se ve na
      propria classe ou nas herdeiras. Quando é privado, só se ve na 
      pripria classe."
;

echo "<br><br>";

echo "<b>Para que serve uma Interface?</b>
      <p>Pode ser usada para organizar o código, é onde definimos padrões,
      que vai ser aplicado em diferentes situações para que esses padrões
      sejam aplicados. A interface diz quais os métodos devem existir, qual
      o tipo de declaração deles e quais parametros devem receber."
;